<% include partials/head %>
<%- include('partials/header', {connected: 'ok'}) %>

<body>
    <p style="display: none;" id="pId" ><%= idChallenge %></p>
    <p style="display:none" id="pError">Une erreur est survenue pendant le chargement du challenge...</p>
    <div class="loader" id="loader" style="top: calc(50vh - 60px); left: calc(50vw - 60px); position:fixed;"></div>
    <!--
    <script src="/socket.io/socket.io.js"></script>
    <script>
        var socket = io.connect('http://localhost:3000');
        socket.on('challengeReady', function(port) {
            window.location.replace("http://localhost:" + port);
        })
    </script>
    -->
    <script src="/js/loading.js"></script>
</body>
<% include partials/footer %>